////////////////////////
mfo compile
向开发获取DEM的gerrit编译方式，尝试本地编译，发现还是有跟之前遇到的问题一样。后来去和开发讨论后，发现是机器问题，换另一个机器后就好了。
之中还遇到强行中断编译后，某些文件夹删除不了（开发说是rsync命令异常中断导致，可能还lock着，要重新执行编译过程，应该就可以删除了）；遇到编译一次后，第二次就不能编译了（提示某个失败），这个是因为某些隐藏文件没有删除成功导致的。

